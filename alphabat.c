#include <stdio.h>
main()
{
    char h;
    printf("Enter the character of required ");
    scanf("%c",&c);
    if( (c>='a' && c<='z') || (c>='A' && c<='Z'))
        printf("%c is an alphabet.",h);
    else
        printf("%c is not an alphabet.",h);

    
}
